import Commands from '../../Papers/CommandPaper/CommandPaper.js';
const cmd = Commands.create({
    name: 'quick',
    description: 'Perintah akan segera tersedia untuk ROTJ.',
    category: 'ROT'
});
